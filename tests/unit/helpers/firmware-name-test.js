import { firmwareName } from 'harvestman-frontend/helpers/firmware-name';
import { module, test } from 'qunit';

module('Unit | Helper | firmware name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = firmwareName([42]);
  assert.ok(result);
});
